# Copy to android/key.properties (gitignored) for release APK signing.
# Generate a keystore: scripts\setup_android_signing.ps1

storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=evolve-release
storeFile=evolve-release.keystore